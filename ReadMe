For installing and running this code, follow the below instructions:

1. Download or clone the repo.
2. Open terminal in base directory.
3. Run "npm install" command to install dependencies.
4. Run "npm start" to start the server.
5. Hit "localhost:3000/search?getQuery" with the query in natral language for results.
